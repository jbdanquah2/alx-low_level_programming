#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two numbers
 * @a: first digit
 * @b: second digit
 *
 * Return: result.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
