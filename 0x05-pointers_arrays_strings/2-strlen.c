#include "main.h"
#include <string.h>

/**
 * _strlen - prints the length of a string
 * @s: string to check length
 * Return: int
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
